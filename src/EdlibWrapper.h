#ifndef EdlibWrapper_h
#define EdlibWrapper_h

#include <cstddef>
#include <string>

size_t getNumMismatches(const std::string& leftSequence, const std::string& rightSequence, const size_t maxMismatches);

#endif
